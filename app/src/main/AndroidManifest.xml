<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ayah2019">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyMedicineActivity"></activity>
        <activity
            android:name=".MainMedicine"
            android:label="@string/title_activity_main_medicine"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".All_medicine" />
        <activity android:name=".Location" />
        <activity android:name=".HowToBuy" />
        <activity android:name=".searchMidicine" />
        <activity android:name=".welcomescreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Ayah2019.data.MedicineAdapter" />
        <activity android:name=".AddMedicine" />
        <activity android:name=".signinActivity" />
        <activity android:name=".signupActivity" />
    </application>

</manifest>